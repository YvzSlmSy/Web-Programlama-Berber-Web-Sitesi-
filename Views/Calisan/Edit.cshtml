@model BerberYonetimSistemi.Models.Calisan

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>Calisan</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="CalisanId" />
            <div class="form-group">
                <label asp-for="CalisanAdi" class="control-label"></label>
                <input asp-for="CalisanAdi" class="form-control" />
                <span asp-validation-for="CalisanAdi" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BerberId" class="control-label"></label>
                <select asp-for="BerberId" class="form-control" asp-items="ViewBag.BerberId"></select>
                <span asp-validation-for="BerberId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
